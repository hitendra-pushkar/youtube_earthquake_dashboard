# This is a Declarative Automation Bundle definition for youtube_earthquake_bundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: youtube_earthquake_bundle
  uuid: 5b06a5d5-46e3-4bc9-af77-f9a28a45be70

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use
  warehouse_id:
    description: WarehouseID

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-d1b02804-095f.cloud.databricks.com
    variables:
      catalog: youtube_earthquake_dashboard
      schema: dev
      warehouse_id: fa4c3cdbe539d618
    presets:
      artifacts_dynamic_version: true
  prod:
    mode: production
    workspace:
      host: https://dbc-d1b02804-095f.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/hitti07@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/hitti07@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: youtube_earthquake_dashboard_prod
      schema: prod
      warehouse_id: fa4c3cdbe539d618
    permissions:
      - user_name: hitti07@gmail.com
        level: CAN_MANAGE
